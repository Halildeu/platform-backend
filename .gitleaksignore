# gitleaks ignore — false-positive fingerprints
#
# Format: <commit-sha>:<file>:<rule-id>:<line>
# OR: <file>:<rule-id> (path-based, ignore specific rule across history)

# Faz 23.2 PR-A — DkimSigner.java contains "-----BEGIN PRIVATE KEY-----" string
# literals as PEM header markers for parsePrivateKey() helper. These are NOT
# leaked secrets; they are PEM format constants used in .replace() calls to
# strip headers before Base64 decode. The actual private key content is
# injected via Vault/ESO at runtime (notify.dkim.private-key-pem).
notification-orchestrator/src/main/java/com/serban/notify/provider/DkimSigner.java:private-key
