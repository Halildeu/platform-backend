# gitleaks ignore — false-positive fingerprints
#
# Format: <commit-sha>:<file>:<rule-id>:<line>
#
# Faz 23.2 PR-A — DkimSigner.java contains "-----BEGIN PRIVATE KEY-----" and
# "-----BEGIN RSA PRIVATE KEY-----" string literals as PEM header markers
# for parsePrivateKey() helper. These are NOT leaked secrets; they are PEM
# format constants used in .replace() / .contains() calls. The actual
# private key content arrives via Vault/ESO injection at runtime
# (notify.dkim.private-key-pem env). Subsequent commit (9ecc38c+) refactored
# to string concatenation to avoid pattern match. These fingerprints cover
# the prior commits in the same PR history.
6b4f59359a909f7c8452c4b2db29d84290f16480:notification-orchestrator/src/main/java/com/serban/notify/provider/DkimSigner.java:private-key:117
4c7228e1f901bd0446c9795707322429361d8b7b:notification-orchestrator/src/main/java/com/serban/notify/provider/DkimSigner.java:private-key:102
4c7228e1f901bd0446c9795707322429361d8b7b:notification-orchestrator/src/main/java/com/serban/notify/provider/DkimSigner.java:private-key:109

# Faz 23.2.A T1.1.8 P0.5 — historical commit suppression (Codex iter-2 absorb).
# Original commit `eaf1d326...` introduced UnsubscribeRevokeServiceIntegrationTest
# with unsplit fixture string `integration-test-signing-secret-32-char-fixed`.
# Subsequent commit `220b542...` split the string for gitleaks compatibility,
# but PR-time gitleaks scans ALL PR commits in history and still flags the
# original commit. Squash-merge will produce a single commit with the SPLIT
# version on main, so post-merge gitleaks (run on main push) will PASS
# naturally — no SHA-bound entry needed for the squashed commit. This entry
# only suppresses the historical PR commit that no longer exists post-squash.
eaf1d326da69d4a059a3392f900a9d67471f076c:notification-orchestrator/src/test/java/com/serban/notify/unsubscribe/UnsubscribeRevokeServiceIntegrationTest.java:generic-api-key:58

# Faz 22.5 BE-021 — historical commit suppression. Original commit
# `4f9f7964...` introduced InstallEvidencePayloadPolicyTest with an
# inline pseudo-JWT fixture (`eyJ...eyJ...sig...`) on line 60. Subsequent
# commit `abaaddfe...` composed the fixture at runtime via string
# concatenation so the source line no longer matches the gitleaks
# `jwt` regex. PR-time gitleaks scans ALL PR commits in history and
# still flags the original commit. Squash-merge will produce a single
# commit with the SPLIT version on main, so post-merge gitleaks (run
# on main push) will PASS naturally — no SHA-bound entry needed for
# the squashed commit.
4f9f796419e979f2110641f7f703200062221c03:endpoint-admin-service/src/test/java/com/example/endpointadmin/security/InstallEvidencePayloadPolicyTest.java:jwt:60

# Faz 22.5 BE-021 follow-up — historical commit suppression
# (Codex 019e7011 iter-1 REVISE absorb). Original commit `7b31c45c...`
# introduced EndpointAgentCommandServiceInstallBranchTest with an
# inline `forbidden.put("licenseKey", "ABCD-1234-EFGH-5678")` test
# fixture on line 114. Subsequent commit `821236e1...` replaced the
# value with the same non-secret placeholder
# (`test-fake-license-marker-no-real-secret`) that
# InstallEvidencePayloadPolicyTest already uses, so the source no
# longer matches the gitleaks `generic-api-key` regex. PR-time
# gitleaks scans ALL PR commits in history and still flags the
# original commit. Squash-merge will produce a single commit with the
# placeholder version on main, so post-merge gitleaks (run on main
# push) will PASS naturally — no SHA-bound entry needed for the
# squashed commit. This entry only suppresses the historical PR
# commit that no longer exists post-squash.
7b31c45cff16c24b0605e006aed4aee656ed95bc:endpoint-admin-service/src/test/java/com/example/endpointadmin/service/EndpointAgentCommandServiceInstallBranchTest.java:generic-api-key:114
