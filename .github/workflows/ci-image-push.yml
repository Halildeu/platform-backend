name: CI - Image Build + GHCR Push

# Faz 19.8 dual-build — Codex AGREE 019dc0ef Option B
# Yeni image isimleri: ghcr.io/halildeu/platform-backend-<service>
# Gitops overlay'leri 19.9'da digest pin ile bu image'lara geçecek.
# Ssot CI paralel kalır (19.10'a kadar) — dual-build safety net.
# This workflow is build-only. Runtime promotion starts with a reviewed,
# digest-pinned GitOps overlay PR and ArgoCD reconciliation.

on:
  push:
    branches: [main]
  workflow_dispatch:
    inputs:
      service:
        description: "Build-only: service image to publish, or all"
        required: false
        default: "all"
        type: choice
        options:
          - all
          - auth-service
          - user-service
          - variant-service
          - core-data-service
          - report-service
          - endpoint-admin-service
          - schema-service
          - permission-service
          - api-gateway
          - discovery-server
          - notification-orchestrator
          - audio-gateway-service
          - meeting-service
          - transcript-service
          - audit-event-consumer-service

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_OWNER: halildeu

jobs:
  build-and-push:
    name: Build + Push ${{ matrix.service }}
    runs-on: ubuntu-latest
    timeout-minutes: 25

    strategy:
      fail-fast: false
      matrix:
        service:
          - auth-service
          - user-service
          - variant-service
          - core-data-service
          - report-service
          - endpoint-admin-service
          - schema-service
          - permission-service
          - api-gateway
          - discovery-server
          - notification-orchestrator
          - audio-gateway-service
          - meeting-service
          - transcript-service
          - audit-event-consumer-service

    steps:
      - name: Checkout
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.service == 'all' || inputs.service == matrix.service }}
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.service == 'all' || inputs.service == matrix.service }}
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.service == 'all' || inputs.service == matrix.service }}
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Derive short SHA
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.service == 'all' || inputs.service == matrix.service }}
        id: sha
        run: echo "short=$(echo ${{ github.sha }} | cut -c1-7)" >> $GITHUB_OUTPUT

      - name: Build + Push image
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.service == 'all' || inputs.service == matrix.service }}
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ${{ matrix.service }}/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/platform-backend-${{ matrix.service }}:sha-${{ steps.sha.outputs.short }}
            ${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/platform-backend-${{ matrix.service }}:latest
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,scope=${{ matrix.service }},mode=max
          provenance: false

      - name: Summary — digest (19.9 pin için kanıt)
        if: ${{ github.event_name != 'workflow_dispatch' || inputs.service == 'all' || inputs.service == matrix.service }}
        run: |
          echo "### ${{ matrix.service }} image push" >> $GITHUB_STEP_SUMMARY
          echo "- **Tag**: \`sha-${{ steps.sha.outputs.short }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Digest**: \`${{ steps.build.outputs.digest }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Image**: \`${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/platform-backend-${{ matrix.service }}:sha-${{ steps.sha.outputs.short }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Pinnable**: \`${{ env.REGISTRY }}/${{ env.IMAGE_OWNER }}/platform-backend-${{ matrix.service }}@${{ steps.build.outputs.digest }}\`" >> $GITHUB_STEP_SUMMARY

      # Faz 25 #832 / ADR-0023: image build+push intentionally ends here.
      # A platform-backend main push must never dispatch the legacy
      # deploy-backend-testai workflow because that workflow directly mutates
      # shared k3d-test Deployments with kubectl set image. Test promotion now
      # starts from a reviewed digest-pinned overlay PR and ArgoCD reconciliation.
      # Residual boundary: the legacy workflow still exists in the GitOps repo
      # and an independently authorized caller can invoke its repository_dispatch
      # event directly. This source change removes only platform-backend's
      # automatic/manual dispatch path; it does not remediate that legacy workflow.
